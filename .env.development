# Same-origin path: the Vite dev proxy (vite.config.ts) forwards "/api" to the
# backend, keeping the session cookie first-party. Override the backend with
# DEV_API_PROXY_TARGET. See .env.example for details.
VITE_API_URL=/api

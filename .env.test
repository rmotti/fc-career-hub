# Test runner only. fetch is mocked in tests, so this host is never contacted —
# it just gives resolveBaseUrl an absolute base so request URLs can be parsed
# with `new URL(...)`. Overrides the same-origin "/api" from .env.local in
# vitest's "test" mode.
VITE_API_URL=https://ample-love-production.up.railway.app
